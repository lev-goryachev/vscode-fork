# vscode-fork — environment variables
# Copy to .env and fill in values for local development.
# In production these are set as platform environment variables (e.g. Cloud Run).

# URL of the Talemo backend (FastAPI).
# Required for auth overlay fetch and CSP connect-src.
TALEMO_BACKEND_URL=https://your-backend-url.example.com
